# Phase C — Offline

Use `mutation_id` already on writes.

Queue local drafts. Show “Not synced”. Never a green saved state for unsynced rows.

Conflict: log is append-only; incidents take server version and add a late comment.

Cache map GeoJSON and briefing.
