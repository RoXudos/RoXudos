# Realtime degraded

1. Confirm polling fallback is on.
2. Tell operators: refresh if in doubt; last-updated time is on the mosaic.
3. Check Supabase realtime connections / quotas.
4. Disable noisy channels if a bug storms the socket.
5. After: write a defect. Do not hide the banner because it looks ugly.
